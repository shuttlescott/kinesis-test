schema:
  - api.graphql
  # The ent.graphql schema was generated by Ent.
  - ./ent.graphql

resolver:
  # Tell gqlgen to generate resolvers next to the schema file.
  layout: follow-schema
  dir: .

# gqlgen will search for any type names in the schema in the generated
# ent package. If they match it will use them, otherwise it will new ones.
autobind:
  - creative-production/ent

models:
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.IntID
  # ent.Noder is the new interface generated by the Node template.
  Node:
    model:
      - creative-production/ent.Noder
  # Custom mapping from GraphQL `CardSuit` to the generated `card.Suit` enum type.
  CardSuit:
    model:
      - creative-production/ent/card.Suit
